{
  "name": "<%= appName %>",
  "version": "0.1.0",
  "author": {
    "name": "<%= authorName %>",
    "email": "<%= authorEmail %>"
  },
  "description": "<%= appDescription %>",
  "scripts": {
    "watch": "babel components -d dist/components -w"
  },
  "devDependencies": {
    "babel-cli": "^6.8.0",
    "babel-preset-es2015-rollup": "^1.1.1",
    "gulp": "^3.9.1",<% if (useTs) { %>    "gulp-typescript": "^2.13.6",<% } %>
    "rollup-plugin-babel": "^2.3.9",
    "rollup-stream": "^1.4.1",
    "vinyl-source-stream": "^1.1.0"
  }
}
